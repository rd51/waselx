# Streamlit Secrets Configuration
# Copy to ~/.streamlit/secrets.toml (local) or set in Streamlit Cloud dashboard

# Flask API URL (change if deploying to different host)
API_URL = "http://localhost:5000"

# Optional: for production deployment on Streamlit Cloud
# API_URL = "https://api.yourdomain.com"  # Update with your production API endpoint
